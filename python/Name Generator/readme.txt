Name Generator is a program that reads first and last names from 
text files and generates a random first and last name. 

The program is to be run on the command line and prints the result 
there:
"python3 nameGenerator.py"